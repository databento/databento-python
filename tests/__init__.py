# Minimal testing procedure from fresh virtual environment
# --------------------------------------------------------
# - Create a new virtual environment with Python 3.7 as the base interpreter
# - pip install pytest (testing framework)
# - pip install pytest-mock (testing framework)
# - pip install aiohttp=={version from setup.py}
# - pip install numpy=={version from setup.py}
# - pip install pandas=={version from setup.py}
# - pip install requests=={version from setup.py}
# - pip install zstandard=={version from setup.py}
